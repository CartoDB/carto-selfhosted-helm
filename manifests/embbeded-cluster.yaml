apiVersion: embeddedcluster.replicated.com/v1beta1
kind: Config
spec:
  # renovate: datasource=github-tags depName=replicatedhq/embedded-cluster versioning=loose
  version: 2.3.1+k8s-1.30
  domains:
    proxyRegistryDomain: registry.self-hosted.carto.com
    replicatedAppDomain: replicated.self-hosted.carto.com
  unsupportedOverrides:
    k0s: |
      config:
        spec:
          api:
            extraArgs:
              service-node-port-range: 80-32767
