{{- if and (.Values.litellm.enabled) (not .Values.cartoConfigValues.onlyRunRouter) }}
{{- $secretVars := list
  "CARTO_SELFHOSTED_INSTANCE_ID"
  "ENCRYPTION_SECRET_KEY"
  "EXPORTS_S3_BUCKET_ACCESS_KEY_ID"
  "EXPORTS_S3_BUCKET_SECRET_ACCESS_KEY"
  "MAPS_API_V3_JWT_SECRET"
  "LITELLM_MASTER_KEY"
  "LITELLM_SALT_KEY"
 -}}
{{- $secretContent := include "carto._utils.generateSecretObjects" (dict "vars" $secretVars "context" $ ) -}}
{{- if $secretContent }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ template "carto.litellm.secretName" . }}
  namespace: {{ .Release.Namespace | quote }}
  labels: {{- include "common.labels.standard" . | nindent 4 }}
    {{- if .Values.commonLabels }}
    {{- include "common.tplvalues.render" ( dict "value" .Values.commonLabels "context" $ ) | nindent 4 }}
    {{- end }}
    app.kubernetes.io/component: litellm
  {{- if .Values.commonAnnotations }}
  annotations: {{- include "common.tplvalues.render" ( dict "value" .Values.commonAnnotations "context" $ ) | nindent 4 }}
  {{- end }}
type: Opaque
data:
{{ $secretContent | nindent 2 }}
{{- if not .Values.litellm.redis.existingSecret.existingSecretName }}
  REDIS_PASSWORD: {{ include "carto.litellm.redisPassword" . | b64enc | quote }}
{{- end }}
{{- if not .Values.litellm.database.existingSecret.existingSecretName }}
{{- if and .Values.litellm.database.password (not .Values.litellm.database.existingSecret.existingSecretPasswordKey) }}
  DATABASE_PASSWORD: {{ include "carto.litellm.databasePassword" . | b64enc | quote }}
{{- end }}
{{- end }}
{{- end }}
{{- end }}