{{- if and (.Values.appConfigValues.aiFeaturesEnabled) (.Values.aiProxy.autoscaling.enabled) }}
apiVersion: autoscaling/v1
kind: HorizontalPodAutoscaler
metadata:
  name: {{ template "carto.aiProxy.fullname" . }}
  namespace: {{ .Release.Namespace | quote }}
  labels: {{- include "common.labels.standard" . | nindent 4 }}
    app.kubernetes.io/component: aiproxy
    {{- if .Values.commonLabels }}
    {{- include "common.tplvalues.render" ( dict "value" .Values.commonLabels "context" $ ) | nindent 4 }}
    {{- end }}
  {{- if .Values.commonAnnotations }}
  annotations: {{- include "common.tplvalues.render" ( dict "value" .Values.commonAnnotations "context" $ ) | nindent 4 }}
  {{- end }}
spec:
  minReplicas: {{ .Values.aiProxy.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.aiProxy.autoscaling.maxReplicas }}
  scaleTargetRef:
    apiVersion: {{ include "common.capabilities.deployment.apiVersion" . }}
    kind: Deployment
    name: {{ template "carto.aiProxy.fullname" . }}
  targetCPUUtilizationPercentage: {{ .Values.aiProxy.autoscaling.targetCPUUtilizationPercentage }}
{{- end }} 