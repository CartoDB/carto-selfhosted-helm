{{- if and (.Values.appConfigValues.aiFeaturesEnabled) (not .Values.cartoConfigValues.onlyRunRouter) }}
apiVersion: v1
kind: Service
metadata:
  name: {{ template "carto.aiProxy.fullname" . }}
  namespace: {{ .Release.Namespace | quote }}
  labels: {{- include "common.labels.standard" . | nindent 4 }}
    app.kubernetes.io/component: aiproxy
    {{- if .Values.commonLabels }}
    {{- include "common.tplvalues.render" ( dict "value" .Values.commonLabels "context" $ ) | nindent 4 }}
    {{- end }}
  annotations:
    {{- if .Values.commonAnnotations }}
    {{- include "common.tplvalues.render" ( dict "value" .Values.commonAnnotations "context" $ ) | nindent 4 }}
    {{- end }}
    {{- if .Values.aiProxy.service.annotations }}
    {{- include "common.tplvalues.render" (dict "value" .Values.aiProxy.service.annotations "context" $) | nindent 4 }}
    {{- end }}
spec:
  type: {{ .Values.aiProxy.service.type }}
  {{- if and .Values.aiProxy.service.clusterIP (eq .Values.aiProxy.service.type "ClusterIP") }}
  clusterIP: {{ .Values.aiProxy.service.clusterIP }}
  {{- end }}
  {{- if and .Values.aiProxy.service.externalTrafficPolicy (ne .Values.aiProxy.service.type "ClusterIP") }}
  externalTrafficPolicy: {{ .Values.aiProxy.service.externalTrafficPolicy }}
  {{- end }}
  {{- if and .Values.aiProxy.service.loadBalancerIP (eq .Values.aiProxy.service.type "LoadBalancer") }}
  loadBalancerIP: {{ .Values.aiProxy.service.loadBalancerIP }}
  {{- end }}
  {{- if and (eq .Values.aiProxy.service.type "LoadBalancer") .Values.aiProxy.service.loadBalancerSourceRanges }}
  loadBalancerSourceRanges: {{- toYaml .Values.aiProxy.service.loadBalancerSourceRanges | nindent 4 }}
  {{- end }}
  ports:
    - port: {{ .Values.aiProxy.service.ports.http }}
      targetPort: http
      protocol: TCP
      name: http
      {{- if and (or (eq .Values.aiProxy.service.type "NodePort") (eq .Values.aiProxy.service.type "LoadBalancer")) .Values.aiProxy.service.nodePorts.http }}
      nodePort: {{ .Values.aiProxy.service.nodePorts.http }}
      {{- else if eq .Values.aiProxy.service.type "ClusterIP" }}
      nodePort: null
      {{- end }}
    {{- if .Values.aiProxy.service.extraPorts }}
    {{- include "common.tplvalues.render" (dict "value" .Values.aiProxy.service.extraPorts "context" $) | nindent 4 }}
    {{- end }}
  selector:
    {{- if .Values.aiProxy.service.labelSelectorsOverride }}
    {{- include "common.tplvalues.render" (dict "value" .Values.aiProxy.service.labelSelectorsOverride "context" $) | nindent 4 }}
    {{- else }}
    {{- include "common.labels.matchLabels" . | nindent 4 }}
    app.kubernetes.io/component: aiproxy
    {{- end }} 
{{- end }}